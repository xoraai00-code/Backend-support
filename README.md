# Base44 Backend Generator

A simple Node.js backend generator inspired by Base44.

## 🚀 Features
- Generates full Express + SQLite backend from a JSON schema  
- Auto-creates tables and CRUD routes  
- Saves generated apps in `/generated_apps/`

## 🧩 Usage
```bash
git clone https://github.com/<your-username>/base44-backend-generator.git
cd base44-backend-generator
npm install
node backendGenerator.js
